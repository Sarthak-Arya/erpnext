[project]
name = "lumina_crm"
authors = [
    { name = "Margx Labs", email = "hello@margxlabs.com" }
]
description = "Lumina CRM theme and branding for ERPNext"
requires-python = ">=3.10"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.bench.frappe-dependencies]
frappe = ">=16.0.0,<17.0.0"
